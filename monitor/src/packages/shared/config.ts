import { alias, version } from '../../../package.json'

export const SDK_NAME: string = alias
export const SDK_VERSION: string = version
export const SDK_SERVER_URL = 'http://localhost:7001/api/error/upload'